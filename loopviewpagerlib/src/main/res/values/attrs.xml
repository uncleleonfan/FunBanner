<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="LoopViewStyle">
        <attr name="dot_radius" format="dimension|reference"/>
        <attr name="enable_auto_loop" format="boolean"/>
        <attr name="loop_interval" format="integer"/>
        <attr name="dot_normal_color" format="color|reference"/>
        <attr name="dot_selected_color" format="color|reference"/>
        <attr name="indicator_bar_background_color" format="color|reference"/>
        <attr name="show_indicator" format="boolean"/>
        <attr name="height_width_ratio" format="float"/>
        <attr name="indicator_bar_height" format="dimension|reference"/>
        <attr name="title_color" format="color|reference"/>
    </declare-styleable>


    <declare-styleable name="CirclePageIndicator">
        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered" format="boolean"/>
        <!-- Color of the filled circle that represents the current page. -->
        <attr name="fillColor" format="color" />
        <!-- Color of the filled circles that represents pages. -->
        <attr name="pageColor" format="color" />
        <!-- Orientation of the indicator. -->
        <attr name="android:orientation"/>
        <!-- Radius of the circles. This is also the spacing between circles. -->
        <attr name="radius" format="dimension" />
        <!-- Whether or not the selected indicator snaps to the circles. -->
        <attr name="snap" format="boolean" />
        <!-- Color of the open circles. -->
        <attr name="strokeColor" format="color" />
        <!-- Width of the stroke used to draw the circles. -->
        <attr name="strokeWidth" format="dimension"/>
        <!-- View background -->
        <attr name="android:background"/>
    </declare-styleable>
</resources>